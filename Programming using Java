Iteration Control Structure - Quiz

Q1. What will be the output of the below program?
javaint i = 1, j = 10;
do { j--; } while (++i < 5);
System.out.println("i = " + i + " and j = " + j);
Answer: a) i = 5 and j = 6

Q2. What will be the output of the below program?
javaint sum = 0;
for (int i = 0, j = 0; i < 5 & j < 5; ++i, j = i + 1) { sum += i; }
System.out.println(sum);
Answer: b) 6

Q3. What will be the output of the below program?
javafor (int i = 0; i < 3; i++) {
    switch (i) {
        case 0: case 1: System.out.println("One "); break;
        case 2: System.out.println("Two ");
    }
}
Answer: d) One One Two

Q4. What will be the output of the below program?
javaint counter = 1;
while (counter < 3) { counter = counter + 1; System.out.println(counter); }
Answer: c) 2 3

Q5. What will be the output of the below program?
javaint number = 28;
for (int num = 25; num < 30; num++) {
    if (number > num) { System.out.println(num); }
    else { System.out.println(num); break; }
}
Answer: b) 25 26 27 28

Q6. What should be the value of num1 and num2 to produce the output as 4?
javawhile (num1 >= 2) {
    if (num1 > num2) { num1 = num1 / 2; }
    else { System.out.println(num1); break; }
}
Answer: c) 16 6

Q7. What will be the output of the below program?


javaint counter = 0;
while (counter <= 7) {
    if (counter % 2 == 0) { counter += 1; continue; }
    else { counter += 1; }
    System.out.println(counter);
}
Answer: d) 2 4 6 8

Q8. What will be the output of the below program?
javaint i = 0;
for (; i <= 3; i++) {
    if (i++ % 2 == 0) { System.out.println("i = " + i); }
}
System.out.println("i after the loop = " + i);
Answer: c) i = 1 i = 3 i after the loop = 4

Q9. What will be the output of the below program?
javaint num1 = 0, num2 = 0;
for (int i = 0; i < 5; i++) {
    if ((++num1 > 2) && (++num2 > 2)) { num1++; }
}
System.out.println(num1 + " and " + num2);
Answer: c) 6 and 3

Q10. What will be the output of the below program?
javafor (int i = 2; i >= i % 2; i--) { System.out.println(i); }
Answer: d) 2 1 0 -1


Introduction to Object Oriented Programming - Quiz

Q1. How many instance variables and objects are there in the following code?
javaBook myBook = new Book();
Book yourBook = new Book();
Book newBook;
Answer: c) instance variables: 4, objects: 2

Q2. What is the output of the following code?
javaDemo test = new Demo();
System.out.println(test.flag);
Answer: c) false


Methods - Quiz

Q1. What will be the output of the below code?
javaif (tester.checkTemp(110))
Answer: b) Data is valid

Q2. What will be the output of the below method?
javavarThree = (double) (varTwo + varOne * (7.0 / 100));
Answer: b) varThree:90.0

Q3. Identify the number of instances and local variables present in the Student class.
javapublic int rollNo;
public String studentName;
public void findGrade(int marks) { char grade; ... }
Answer: a) 2 instance variables, 2 local variables

Q4. What will the output of the below code?
javaint res = tester.subtract(b, a);
Answer: c) 10

Q5. What will be the output of the below code?
javatester.add(a, b);
System.out.println(sum);
Answer: b) 0 Sonnet 4.6Extended


Constructors - Quiz


Q1. What will be the output of the below code?
javaDemo() { value = 40; }
Demo demo = new Demo();
System.out.println(demo.value);
Answer: c) 40

Q2. What will be the output of the below code?
javaDemo demo = new Demo();
Answer: d) compilation error: class Demo has no default constructor

Q3. Which of the following options will create an object for the class?
javaCircle(double r) { radius = r; }
Answer: a) Circle c = new Circle(10.5)

Q4. Which of the following is/are true about constructors in Java?
Answer:

a) The name of the constructor should be same as the class name.
b) If you don't define a constructor for a class, a default parameterless constructor is automatically provided by the compiler.
c) The default constructor initializes all the instance variables to default values of the data types.
 Sonnet 4.6Extended


this Keyword - Quiz


Q1. What will be the output of the below code?
javaTester(int num1, int num2) {
    num1 = num1;
    num2 = num2;
}
Answer: c) 0 0

Memory Management - Quiz

Q1. Which of the following statements are true?
Answer:

a) You cannot be certain at what point of time garbage collection will occur.
c) An object will be eligible for garbage collection after the execution of the method is over.


Q2. How many objects and references have been created?
javaStudent student3 = student1;
Student student4 = null;
Answer: c) objects: 2, references: 4

Q3. What will be the output of the following code?
javaTrainee trainee = null;
System.out.println(trainee.id);
Answer: c) runtime error

Q4. How many objects will be eligible for garbage collection?
javastudent3 = null;
student1 = student3;
Answer: c) 2

Q5. Which statement is true after executing Line 1?
javaaccount1 = new Account(); // Line 1
Answer: b) account1 and account2 are pointing to different objects

Q6. How many objects will be eligible for garbage collection?
javaaccount2 = account1 = null;
Answer: a) 3

Encapsulation, Abstraction and Access Modifiers - Quiz


Q1 of 4

What is the output of the below code?

class A {
	private void displayMessage() {
		System.out.println("Welcome");
	}
}

public class B {
	public static void main(String args[]) {
		A message = new A();
		message.displayMessage();
	}
}
 

Welcome
null
No output
Compilation error: The method displayMessage() from type A is not visible
Q2 of 4

What is the output of the below code?

class A {
	public void displayMessage() {
		System.out.println("Welcome");
	}
}

public class B {
	public static void main(String args[]) {
		A message = new A();
		message.displayMessage();
	}
}
 

Welcome
null
No output
Compilation error: The method displayMessage() from type A is not visible
Q3 of 4

Which of the following statements are correct?

i. public methods of a class can be accessed anywhere in a program

ii. private methods of a class can be accessed only inside the class

iii. members created without any access modifier will have default access

Only i and ii
All i, ii and iii
Only i and iii
Only ii and iii
Q4 of 4

What is the output of the below code?

class Purchase {
	private String mobileName;
	private int price;
	private int stock;

	public void purchaseMobile() {
		stock = stock - 1;
	}

	public String getMobileName() {
		return mobileName;
	}

	public void setMobileName(String mobileName) {
		this.mobileName = mobileName;
	}

	public int getPrice() {
		return price;
	}

	public void setPrice(int price) {
		this.price = price;
	}

	public int getStock() {
		return stock;
	}

	public void setStock(int stock) {
		this.stock = stock;
	}

}

public class Tester {
	public static void main(String[] args) {
		Purchase mobile = new Purchase();
		mobile.setMobileName("Moto");
		mobile.setPrice(200);
		mobile.purchaseMobile();
		System.out.println(mobile.getStock());
	}
}
 

Compilation error: stock is not initialized
null
0
-1


String - Quiz

Q1. What will be the output of the below code?
javastr.charAt(str.length())
Answer: d) runtime error: index out of bounds

Q2. What will be the output of the below code?
javaString num = "10";
int n = 20;
System.out.println(num + n);
Answer: b) 1020

Q3. What will be the output of the below code?
javaSystem.out.println(s1 = s2);
Answer: b) abcd

Q4. What will be the output of the below code?
javaSystem.out.println(s1.equals(s2));
System.out.println(s1 == s2);
Answer: a) true false

Q5. What will be the output of the below code?
javaSystem.out.println(p1.equals(passengerFirstName));
System.out.println(p1.equalsIgnoreCase(passengerFirstName));
Answer: c) false true

Q6. What will be the output of the below code?
javainput.toUpperCase();
System.out.println(input);
Answer: c) welcome to string functions


Array - Quiz

Q1. Which of the following are valid array declarations?
Answer: c) 2, 3, 5, 7

Q2. Which of the following statements is/are valid?
Answer:

a) int score[][] = new int[2][];
b) int score[][] = new int[2][2];
d) int score[][] = new int[2][]; score[0] = new int[2];


Q3. What will be the output of the below code?
javan = arr[arr[n] / 2];
System.out.println(arr[n] / 2);
Answer: d) 1

Q4. What will be the output of the below code?
javaif (sale > average) count++;
break;
Answer: c) Average sales: 10.333333 Sales above average: 0

Q5. What will be the output of the below code?
javaif (a[i][j] % 2 == 0) break;
sum += a[i][j];
Answer: b) sum = 11

Q6. What will be the output of the below code?
javaif (i % 3 != 0) { --i; }
System.out.println(a[i]);
Answer: c) 19 17 15

Q7. What will be the output of the below code?
javafor (int j = 1; j < inputArray[i].length - 1; j++)
    total *= inputArray[i][j];
Answer: a) Result = 72

Q8. What will be the output of the below code?
javaif (i == j) continue;
total += numbers[i][j];
Answer: d) Result = 31

Code Analysis - Quiz

Q1. Which of the following code snippets do not have any naming convention violation? Answer:

a)public class PMDDemo {...}
d) public class PmdDemo {...}
e) public void pmdDemo() {...}


Static - Quiz

Q1. How many static variables, local variables and instance variables are present?
javaprivate static int numberOfCars = 0;
private String vehicleNumber, color;
public void display(String name)
Answer: a) static variables: 1; instance variables: 2; local variables: 1

Q2. Which of the following is/are correct?
Answer:

c) Static blocks get executed only once.
d) Static variables and methods can be accessed without creating an object of the class.


Q3. Choose the correct option based on the code given below.
javastatic { name = "john"; } // line 1
Answer: a) Compilation error in Line 1 as a non-static member cannot be accessed inside a static block.

Q4. What will be the output of the code given below?
javaprivate static int id;
id = ++counter;
Answer: b) 3 3 3

Q5. Predict the output of the code given below.
javaPrinter p = null;
p.display(...);
Answer: c) Static block in Tester class; In main; Static block in Printer class; The value of sample variable is: 2

Q6. What will be the output of the below code?
javapublic static void main(String args[]) {
    System.out.println(this.var);
}
Answer: c) compilation error: cannot use 'this' inside main

Aggregation - Quiz

Q1. What will be the output of the below code?
javaBuilding b = new Building();
// calls Building() which calls new Room(20)
Answer: c) Room created 


Association - Quiz

Q1. What will be the output of the code given below?
javaSystem.out.println("Student appeared for exam using PC Id: " + c.id);
Answer: d) compilation error: id is private and cannot be accessed directly in another class

Q2. Replace //missing code so that findSum() calculates the sum of all objects of array b.
javafor (B item : b) { // missing code }
Answer: b) sum += item.num

Q3. What will be the output of the below code?
javap1.updateCabin(c2);
// both p1 and p2 point to same c1 object
this.cabin.cabinNumber = c.cabinNumber;
Answer: a) 122 122

Q4. Which of the below relations can be bidirectional?
Answer: b) Association

**Inheritance - Quiz**

Q1. Predict the output of the following code.
javaPremiumCustomer premiumCustomer = new PremiumCustomer();
Answer: a) Customer Regular Customer Premium Customer

Q2. In the constructor of DigitalCamera class, the call to the base class constructor ____________.
Answer: b) must be made in the first line of the constructor

Q3. Predict the output of the following code.
javasuper(var2); // var2=20
this.var3 = 40;
sum = getVar1() + getVar2() + var3; // 10+20+40
Answer: b) Result: 70

Q4. Which of the following statements are true about constructors in Java?
Answer:

a) If constructor for a class is not defined, then the compiler automatically provides a default parameterless constructor.
b) The name of the constructor should be same as the class name.
c) The parameterless constructor of the parent class is invoked implicitly by the child class constructors if super() is not explicitly used in the child class constructor.
d) A call to a parent class constructor must be the first statement inside the child class constructor.


Method Overloading - Quiz

Q1. What will be the output of the following code?
javatester.evaluate(4, 2.5f);
// int and float -> matches (float, float)
Answer: b) Second evaluate method invoked 6.5

Q2. What is the output of the following code?
javatester.evaluate(4.5, 2.5);
// double arguments, no matching method
Answer: b) Compilation error: The method evaluate(int, int) in the type Tester is not applicable for the arguments (double, double)

Q3. Which of the following are overloaded methods?
Answer: b) Only a and c

a is valid: different number of parameters
b is invalid: same method signature, only return type differs
c is valid: different order of parameter types

Constructor Overloading - Quiz

Q1. Which of the following statements is/are correct?
Answer: a) Only (i), (iii) and (iv)

Q2. What will be the output of the code given below?
javaEmployee employee1 = new Employee("John", 101); // city = null
Employee employee2 = new Employee();             // name = null, empId = 0
Answer: b) John 101 null null 0 New York

Q3. What will be the output of the code given below?
javaEmployee(String name, int empId) {
    this(); // calls Employee() which sets city = "New York"
    this.name = name;
    this.empId = empId;
}
Answer: c) John 101 New York null 0 New York Sonnet 4.6Extended

Method Overriding - Quiz

Q1. What will be the output of the following code snippet?
javaPerson person = new Student();
person.print(); // runtime polymorphism, calls Student's print()
Answer: a) In Person class In Student class Student name John

Q2. What will be the output of the following code snippet?
javaPerson c = new Customer(20000);
c.getDetails(); // calls Customer's getDetails()
Answer: b) Maddy 34 20000

Q3. What will be the output of the following code snippet?
javaBike hondaBike = new Honda();
hondaBike.speedlimit; // variable, no polymorphism, refers to Bike's variable
Answer: a) 90 150 90

Q4. Predict the output of the code given below.
javaBank bank = new Bank();
bank.getBankName(); // private method, no overriding
Answer: b) Bank

Q5. What will be the output of the following code snippet?
javaa.method1() + c.method1() + c.method2()
// 20 + 30 + 20 = 70
Answer: c) 70

Q6. What will be the output of the following code snippet?
javasuper(10); // sets Loan's interestRate = 10
interestRate = super.interestRate; // = 10
Answer: b) Request for loan Request for home loan 10

Q7. Can you apply more than one annotation on any method?
Answer: a) Yes

final keyword - Quiz

Q1. Identify the correct declaration of a final variable.
Answer: b) private final double PI = 3.14

Q2. What will be the output of the below code?
javafinal int num = 100;
this.num = 50; // trying to change final variable
Answer: c) Compilation error as the value of a final variable cannot be changed


Abstract - Quiz

Q1. Which of the following class creation prevents creating a child class of the Example class?
Answer: c) public final class Example {}

Q2. Choose the correct statements from the statements given below.
Answer:

a) Abstract classes can have concrete methods.
c) Concrete sub classes of an abstract class must implement all the abstract methods.
d) Abstract classes can have abstract methods.
e) Subclasses of abstract classes may or may not be abstract.


Q3. Predict the output of the following code.
javaB obj = new B();
obj.disp(); // calls concrete method from parent class A
Answer: b) Concrete method of parent class

Interface - Quiz

Q1. What will happen on executing the code given below?
javaBankOperations subBranch = new SubBranch();
subBranch.deposit();
Answer: d) Code will get executed successfully and display "Amount deposited"

Q2. Which of the following is/are true for classes and interfaces?
Answer:

c) "Account extends SavingsAccount" is correct if "Account and SavingsAccount both are either interfaces or classes"
d) "SavingsAccount implements Account" is correct if and only if "Account is an interface" and "SavingsAccount is a class"
 Sonnet 4.6Extended

Exception - Quiz

Q1. What will be the output of the below program?
javacatch (ParentClass p) { ... }
catch (ChildClass e) { ... } // unreachable
Answer: d) Compilation error because the catch block of a parent class exception must appear after the catch block of a child class exception

Q2. Which statements could be inserted to allow the code to compile and run?
Answer:

a) throw new ArrayIndexOutOfBoundsException();
c) throw new NullPointerException();


Q3. What will be the output of the below program?
javadisplay(); // throws RuntimeException, caught by catch(Exception e)
Answer: c) main inside display() caught finally end

Q4. What will be the output of the below program?
javathrow new Error(); // Error is not caught by Exception, caught by Throwable
Answer: d) CDE

Q5. What will be the output of the below program?
javathrow new RuntimeException(); // unchecked exception, no throws declaration needed
Answer: a) An exception of type RuntimeException will be thrown.


Package - Quiz

Q1. Consider the below package structures and choose the correct options:
Answer:
a) Class B can access variable b of A
b) Class B can access variable c of A (same package, default access)
e) Class C can access variable d of A (public access)
f) Class D can access variable b of A (different package but extends A, protected access allowed)


Q2. What are the possible access modifiers for the display() method in class B?
Answer:

c) public
d) protected


Q3. Which of the following statements is/are correct?
Answer:

a) public methods of a class can be accessed anywhere in a program.
b) private methods of a class can be accessed only within the class.
d) protected methods of a class can be inherited by sub classes and can become public in the sub classes.


Q4. An _______________ statement can be used to access the classes and interfaces of a different package.
Answer: b) import

Unit Testing and Code Coverage - Quiz

Q1. Which of the following is/are correct?
Answer:

a) A unit test case is a part of code which ensures that another part of code (method) works as expected.
b) A formal written unit test case is characterized by a known input and by an expected output, which is decided before the test is executed.
d) A unit test case compares the expected output with actual output.


Q2. Which of the following is a valid way of declaring an Expected Exception object?
Answer: b) @Rule public ExpectedException expectedException = ExpectedException.none();

Q3. Select the annotations that are used for creating test suites.
Answer:

a) @RunWith(Suite.class)
b) @Suite.SuiteClasses()
 Sonnet 4.6Extended


Recursion - Quiz

Q1. What changes should be made to the method to work correctly?
Answer: d) replace Line2 with return (num + sum(num-1))

Q2. Which of the below methods correctly implement the formula?
Answer:
c) public int square(int n) { if(n==1) return 1; else return (square(n-1)+(2*n-1)); }
d) public int square(int n) { if(n==0) return 0; else return (square(n-1)+(2*n-1)); }


Q3. What does the below method do?
javareturn (x * fun(x, y - 1)); // base case y==0 returns 1
Answer: b) x^y

Q4. What must be written in place of missing code?
javareturn (num1 + multiply(num1, num2 - 1));
**Answer: b) num2==0**

Q5. How many times will divide() be called for num = 4321?
divide(4321) → divide(432) → divide(43) → divide(4) → divide(0)
= 5 times total
Answer: a) 5

Q6. What will be the output for num1=5 and num2=1?
calc(5,1) = 5+1+calc(4,2)
calc(4,2) = 4+2+calc(3,3)
calc(3,3) = 0  (num2 == num1)
= 6 + 6 + 0 = 12
Answer: c) 12

Q7. What will be the output for divide(10)?
10%2=0 → divide(5)
5%2=1  → divide(2)
2%2=0  → divide(1)
1%2=1  → divide(0) → returns 0
Output: 0101
Answer: b) 0101

Regular Expression - Quiz

Q1. Which strings match: Hello (World|Program) .?
Answer:
a) Hello World !
d) Hello Program
f) Hello Program .


Q2. Which strings match: [0-9]+ Pre?mium or{3}der
Answer:
a) 3445 Premium orrrder
b) 57696 Prmium orrrder


Q3. Which strings match: [\\w]+ [\\w]+
Answer: a) Steve FoodOrder_12

Q4. Which strings match: [\\D]+-[A-Z]+Ab\\*[0-9]{3,5}
Answer: d) Steve-FOODORDERAb*1123

Q5. Which regex matches Steve@abc.co.in?
Answer: 
a) \w+@abc\.(co\.in)
c) [\\w+@]+[\\D]+

Q6. Which regex matches CD*56FT*A*54?
Answer: d) \w{2}\\d{2}\w{2}\\w{1}\*\d{2}
\w{2}=CD, \*=*, \d{2}=56, \w{2}=FT, \*=*, \w{1}=A, \*=*, \d{2}=54
